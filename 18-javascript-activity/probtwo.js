const testScore1 = 85;
const testScore2 = 92;
const testScore3 = 78;

const averageScore = (testScore1 + testScore2 + testScore3) / 3;

const roundedAverageScore = Math.round(averageScore);

console.log("Average Test Score:", roundedAverageScore);